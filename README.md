# SSR Vue JS app boilerplate

* Install the app : 
run npm install : 'npm i'

Build /dist files : 
`npm run dev`

* Lunch app :
`npm run start`

* go to : [`http://localhost:8000`](http://localhost:8000)